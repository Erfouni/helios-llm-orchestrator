interface:
  display_name: "Helios"
  short_description: "Route models and run reviewed AI projects"
  default_prompt: "Use $helios to plan a complex project, show the reviewed task graph, and wait for my approval before execution."
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
